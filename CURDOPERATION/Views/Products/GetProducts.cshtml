@model IEnumerable<CURDOPERATION.Models.ProductsModel>

@{
    ViewBag.Title = "GetProducts";
}

<h2>GetProducts</h2>

<p>
    @Html.ActionLink("Create New", "AddProducts")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.productName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ProductDesciption)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ProductColor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ProductListPrice)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.productName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ProductDesciption)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ProductColor)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ProductListPrice)
        </td>
        <td>
            @Html.ActionLink("UpdateProduct", "UpdateProduct", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
